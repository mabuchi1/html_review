.like-link{:id => "like-link-#{blog.id}"}
  - if current_user.likes.find_by(blog_id: blog.id)
    = link_to unlike_path(blog.id), method: :delete, remote: true do
      .iine__button
        ❤️#{blog.likes.count}
  - else
    = link_to like_path(blog.id), method: :post, remote: true do
      .iine__button
        ♡️#{blog.likes.count}